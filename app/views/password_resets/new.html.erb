<% provide(:title, "Forgot password") %>
<h2>パスワードをお忘れの方</h2>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_with(url: password_resets_path, scope: :password_reset,
                    local: true) do |f| %>
      <%= f.label :メールアドレス %>
      <%= f.email_field :email, placeholder: "ご登録されたメールアドレス", class: 'form-control' %>
      <p><small>*ご登録されたメールアドレスにパスワード再設定のご案内が送信されます。</small></p>
      <%= f.submit "送信する", class: "btn btn-primary mt-3" %>
    <% end %>
  </div>
</div>