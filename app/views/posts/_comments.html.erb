<% comments.each do |comment| %>
  <div class="comment">
    <p class="bg-light"><%= link_to comment.user.name, user_path(comment.user.name) %><small> ・ <%= time_ago_in_words comment.created_at %> ago</small></p>
    <%= simple_format comment.message %>
  </div>
<% end %>